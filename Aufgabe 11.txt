1. Homogene Arrays können nur einen Datenytypen speichern
 Heterogene dagegen mehrere.
2.Keine Ahnung
3. Die Eigenschaften des Objekts werden willkürlich durchlaufen
	key=Variable
	query = Objekt dessen Eigenschaft durchlaufen wird
4.Ist eine Freigabeeinstellung
	--> Inhalte werden hierbei allen zur Verfügung gestellt
5.Damit werden Daten vom Server und Nutzer ausgetauscht, ohne dass die Seite neu geladen werden muss.
So kann die Seite verändert werden, ohne neu geladen werden zu müssen

6.Wie in A5 beschrieben können mit Ajax Daten dem Server gesendet werden bzw. er kann diese erhalten
ohne die Seite neu laden zu müssen